server:
  port: 8080

spring:
  application:
    name: job-portal

  datasource:
    url: jdbc:mysql://localhost:3306/jobportal?useSSL=false&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: jobuser
    password: jobpass123

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

  jackson:
    serialization:
      fail-on-empty-beans: false


app:
  jwt:
    secret: "bXktc2VjcmV0LWtleS1mb3Itam9iLXBvcnRhbC1wcm9qZWN0LTI1Ni1iaXQtbG9uZw=="
    access-token-expiry-ms: 3600000
    refresh-token-expiry-days: 7

  upload:
    dir: ./uploads
    max-file-size: 5MB
    allowed-types: application/pdf,image/jpeg,image/png

  cors:
    allowed-origins: http://localhost:3000,http://localhost:5500
